<?php
declare(strict_types=1);

/**
 * Modelo do arquivo de credenciais do formulário de contato.
 *
 * Copie este arquivo para "config.php" (mesma pasta) e preencha o valor
 * abaixo. O config.php de verdade NUNCA deve ser commitado no git — está
 * listado no .gitignore justamente para isso. Suba o config.php para o
 * servidor separadamente, por FTP.
 */

// Senha de app gerada em myaccount.google.com (Segurança > Verificação em
// duas etapas > Senhas de app) para a conta gerencia@npgcapital.com.br.
// 16 caracteres, sem espaços.
define('SMTP_APP_PASSWORD', 'COLE_A_SENHA_DE_APP_AQUI');
